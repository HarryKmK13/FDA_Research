# Notebooks

- `archive/original_research_notebook.ipynb` preserves the exploratory notebook used during the initial analysis.

The reproducible workflow is now in `src/fda_research/analyze_2011.py`. Keeping the exploratory notebook in `archive/` makes the project history transparent while giving readers a cleaner entry point.
